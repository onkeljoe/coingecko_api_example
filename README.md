# coingecko_api_example

This scripts show a test usage of coingecko API.

# start

run app.py

# dependencies

external module "requests"
This ist automatically installed if you use pipfile. You can add manually by:

```sh
pip install requests
```
